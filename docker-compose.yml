services:
  app:
    build:
      context: .
      target: development
    ports:
      - '5173:5173'
    environment:
      CHOKIDAR_USEPOLLING: 'true'
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    stdin_open: true
    tty: true

volumes:
  node_modules:
